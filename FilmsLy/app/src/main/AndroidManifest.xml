<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filmsly">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmsLy">
        <activity android:name=".Results"></activity>
        <activity android:name=".CategoriesView" />
        <activity android:name=".Home" />
        <activity android:name=".QuestionThreeActorsAndWhoTheyPlay" />
        <activity android:name=".QuestionTwoDirectorsAndWriters" />
        <activity android:name=".QuestionOneFamousWords" />
        <activity android:name=".QuestionOneActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>